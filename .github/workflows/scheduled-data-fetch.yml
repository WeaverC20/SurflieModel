name: Scheduled Data Fetch

on:
  schedule:
    # Run every 6 hours
    - cron: '0 */6 * * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  fetch-noaa:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e packages/python/common
          pip install -r data/pipelines/requirements.txt

      - name: Fetch NOAA data
        env:
          NOAA_API_KEY: ${{ secrets.NOAA_API_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          # TODO: Implement data fetching script
          echo "Fetching NOAA data..."
          # python data/pipelines/fetch_all.py

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: fetched-data
          path: data/raw/
          retention-days: 7
