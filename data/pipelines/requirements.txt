httpx>=0.26.0
beautifulsoup4>=4.12.3
pandas>=2.1.4
numpy>=1.26.0
python-dotenv>=1.0.0

# GRIB2 parsing for NOAA Wave Watch 3, GFS, and RTOFS
# eccodes-python includes pre-built binaries - no system dependencies needed!
eccodes>=1.7.0
cfgrib>=0.9.10
xarray>=2023.1.0

# NetCDF parsing for WCOFS
netCDF4>=1.6.0

# Tile generation dependencies
Pillow>=10.0.0  # Image processing for tiles
shapely>=2.0.0  # Geographic shapes for nearshore zones
scipy>=1.11.0   # Interpolation for tile rendering

# Zarr storage for forecast data
zarr>=2.16.0              # Chunked array storage
fsspec>=2023.12.0         # Filesystem abstraction (local, S3, GCS)
s3fs>=2023.12.0           # S3 backend for fsspec (optional, for cloud)
gcsfs>=2023.12.0          # GCS backend for fsspec (optional, for cloud)
