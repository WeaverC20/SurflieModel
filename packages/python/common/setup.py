"""Setup script for wave-forecast-common"""

from setuptools import setup, find_packages

setup(
    name="wave-forecast-common",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.11",
)
